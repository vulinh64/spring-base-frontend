# `app/category`

This route area groups pages that list posts belonging to one category. The category slug and optional page number are represented by nested dynamic route folders.

Category listings are public, SEO-sensitive Server Components. Keep data fetching in the server API layer.
