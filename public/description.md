# `public`

This folder contains assets served directly from the site root, including the favicon, shared SVG icons, and bundled JetBrains Mono font files.

Reference files here with root-relative URLs such as `/favicon.svg` or `/fonts/...`. Keep only static, deployable assets here; source code and generated build output belong elsewhere.
