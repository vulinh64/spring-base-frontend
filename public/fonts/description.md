# `public/fonts`

This folder contains the regular, bold, italic, and bold-italic JetBrains Mono WOFF2 files used for code and numeric display.

The font faces are declared in `app/globals.css`. Keep filenames and declarations synchronized if these assets are replaced.
