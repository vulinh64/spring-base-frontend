# `/me`

This authenticated client route displays the current user's account information and roles from `AuthProvider`.

The page is wrapped in `AuthGuard`. Role badges are informational UI only; backend services must enforce authorization.
